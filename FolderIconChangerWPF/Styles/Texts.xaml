<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ap="clr-namespace:FolderIconChangerWPF.AttachedProperties"
    xmlns:conv="clr-namespace:FolderIconChangerWPF.ValueConverters"
    xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
    xmlns:lex="http://wpflocalizeextension.codeplex.com"
    xmlns:mm="http://metro.mahapps.com/winfx/xaml/controls"
    xmlns:mmConverters="clr-namespace:MahApps.Metro.Converters;assembly=MahApps.Metro"
    xmlns:sys="clr-namespace:System;assembly=mscorlib">

    <Style x:Key="BaseTextBlockDarkStyle" TargetType="{x:Type TextBlock}">
        <Setter Property="FontFamily" Value="{StaticResource LatoRegular}" />
        <Setter Property="FontSize" Value="{StaticResource FontSizeRegular}" />
        <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
        <!--<Setter Property="Background" Value="{StaticResource GrayBrush10}" />-->
    </Style>
    <Style x:Key="BaseTextBoxStyle" TargetType="{x:Type TextBox}">
        <Setter Property="Background" Value="{DynamicResource GrayBrush8}" />
        <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
    </Style>
    <Style
        x:Key="HeaderText"
        BasedOn="{StaticResource BaseTextBlockStyle}"
        TargetType="{x:Type TextBlock}">
        <Setter Property="Foreground" Value="{DynamicResource GrayBrush2}" />
        <Setter Property="HorizontalAlignment" Value="Center" />
        <Setter Property="Margin" Value="0,6" />
        <Setter Property="VerticalAlignment" Value="Center" />
    </Style>
    <Style
        x:Key="TextBoxMedium"
        BasedOn="{StaticResource BaseTextBoxStyle}"
        TargetType="{x:Type TextBox}">
        <Setter Property="BorderBrush" Value="{DynamicResource AccentBaseColorBrush}" />
        <Setter Property="BorderThickness" Value="0,0,0,1" />
        <Setter Property="FontFamily" Value="{StaticResource LatoRegular}" />
        <Setter Property="FontSize" Value="{StaticResource FontSizeXXLarge}" />
        <Setter Property="Margin" Value="0,5,0,5" />
        <Setter Property="Padding" Value="10" />

        <!--<Setter Property="Background" Value="{StaticResource GrayBrush8}" />
        <Setter Property="Foreground" Value="{StaticResource TextBrush}" />-->
        <!--<Setter Property="IsEnabled" Value="{TemplateBinding ap:IsBusyProperty.Value, Converter={conv:BooleanToVisiblityConverter}}"/>-->

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type TextBoxBase}">
                    <Grid>
                        <Border
                            x:Name="border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            SnapsToDevicePixels="True">
                            <ScrollViewer
                                x:Name="PART_ContentHost"
                                Focusable="False"
                                HorizontalScrollBarVisibility="Hidden"
                                VerticalScrollBarVisibility="Hidden" />
                        </Border>
                    </Grid>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="border" Property="Opacity" Value="0.56" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource AccentColorBrush}" />
                        </Trigger>
                        <Trigger Property="IsKeyboardFocused" Value="True">
                            <Setter Property="Effect">
                                <Setter.Value>
                                    <DropShadowEffect
                                        BlurRadius="30"
                                        Direction="280"
                                        RenderingBias="Performance"
                                        Color="{DynamicResource AccentColor}" />
                                </Setter.Value>
                            </Setter>
                            <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource AccentColorBrush}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style
        x:Key="TextBoxWithHintLarge"
        BasedOn="{StaticResource BaseTextBoxStyle}"
        TargetType="{x:Type TextBox}">
        <Setter Property="BorderBrush" Value="{DynamicResource AccentBaseColorBrush}" />
        <Setter Property="BorderThickness" Value="0,0,0,1" />
        <Setter Property="FontFamily" Value="{StaticResource LatoRegular}" />

        <Setter Property="FontSize" Value="{StaticResource FontSizeXXLarge}" />
        <Setter Property="Margin" Value="0,5,0,5" />
        <Setter Property="Padding" Value="10" />

        <!--<Setter Property="Background" Value="{StaticResource ForegroundLightBrush}" />
        <Setter Property="Foreground" Value="{StaticResource ForegroundVeryDarkBrush}" />-->
        <!--<Setter Property="IsEnabled" Value="{TemplateBinding ap:IsBusyProperty.Value, Converter={conv:BooleanToVisiblityConverter}}"/>-->

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type TextBoxBase}">

                    <Grid>

                        <Border
                            x:Name="border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            SnapsToDevicePixels="True">
                            <ScrollViewer
                                x:Name="PART_ContentHost"
                                Focusable="False"
                                HorizontalScrollBarVisibility="Hidden"
                                VerticalScrollBarVisibility="Hidden" />
                        </Border>

                        <TextBlock
                            x:Name="placeholder"
                            Padding="{TemplateBinding Padding}"
                            HorizontalAlignment="{TemplateBinding HorizontalAlignment}"
                            VerticalAlignment="Center"
                            FontFamily="{StaticResource LatoThin}"
                            Foreground="{DynamicResource GrayBrush5}"
                            IsHitTestVisible="False"
                            Text="{TemplateBinding Tag}">
                            <TextBlock.Style>
                                <Style TargetType="{x:Type TextBlock}">
                                    <Setter Property="Visibility" Value="Hidden" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Text, RelativeSource={RelativeSource TemplatedParent}}" Value="">
                                            <Setter Property="Visibility" Value="Visible" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>

                        </TextBlock>

                    </Grid>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="border" Property="Opacity" Value="0.56" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource AccentColorBrush}" />
                        </Trigger>
                        <Trigger Property="IsKeyboardFocused" Value="True">
                            <Setter Property="Effect">
                                <Setter.Value>
                                    <DropShadowEffect
                                        BlurRadius="30"
                                        Direction="280"
                                        RenderingBias="Performance"
                                        Color="{DynamicResource AccentColor}" />
                                </Setter.Value>
                            </Setter>
                            <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource AccentColorBrush}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>

    </Style>

    <Style x:Key="PasswordBoxWithHintLarge" TargetType="{x:Type PasswordBox}">
        <Setter Property="Background" Value="{DynamicResource GrayBrush8}" />
        <Setter Property="BorderBrush" Value="{StaticResource AccentBaseColorBrush}" />
        <Setter Property="BorderThickness" Value="0,0,0,1" />
        <Setter Property="FontFamily" Value="{StaticResource LatoRegular}" />

        <Setter Property="FontSize" Value="{StaticResource FontSizeXXLarge}" />

        <!--<Setter Property="Background" Value="{StaticResource ForegroundLightBrush}" />
        <Setter Property="Foreground" Value="{StaticResource ForegroundVeryDarkBrush}" />-->
        <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
        <Setter Property="Margin" Value="0,5,0,5" />
        <Setter Property="Padding" Value="10" />
        <!--<Setter Property="IsEnabled" Value="{TemplateBinding ap:IsBusyProperty.Value, Converter={conv:BooleanToVisiblityConverter}}"/>-->

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type PasswordBox}">

                    <Grid>

                        <Border
                            x:Name="border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            SnapsToDevicePixels="True">
                            <ScrollViewer
                                x:Name="PART_ContentHost"
                                Focusable="False"
                                HorizontalScrollBarVisibility="Hidden"
                                VerticalScrollBarVisibility="Hidden" />
                        </Border>

                        <TextBlock
                            x:Name="placeholder"
                            Padding="{TemplateBinding Padding}"
                            HorizontalAlignment="{TemplateBinding HorizontalAlignment}"
                            VerticalAlignment="Center"
                            FontFamily="{StaticResource LatoThin}"
                            FontSize="{TemplateBinding FontSize}"
                            Foreground="{StaticResource GrayBrush5}"
                            IsHitTestVisible="False"
                            Text="{TemplateBinding Tag}"
                            Visibility="{TemplateBinding ap:HasTextProperty.Value, Converter={conv:VisibilityWhenFalseConverter}}" />

                    </Grid>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="border" Property="Opacity" Value="0.56" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource AccentColorBrush}" />
                        </Trigger>
                        <Trigger Property="IsKeyboardFocused" Value="True">
                            <Setter Property="Effect">
                                <Setter.Value>
                                    <DropShadowEffect
                                        BlurRadius="30"
                                        Direction="280"
                                        RenderingBias="Performance"
                                        Color="{DynamicResource AccentColor}" />
                                </Setter.Value>
                            </Setter>
                            <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource AccentColorBrush}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>

        <Setter Property="ap:MonitorPasswordProperty.Value" Value="True" />

    </Style>

    <Style
        x:Key="SpinningText"
        BasedOn="{StaticResource BaseTextBlockStyle}"
        TargetType="{x:Type TextBlock}">

        <Setter Property="FontFamily" Value="{StaticResource FontAwesome}" />
        <Setter Property="RenderTransform">
            <Setter.Value>
                <RotateTransform />
            </Setter.Value>
        </Setter>
        <Setter Property="RenderTransformOrigin" Value="0.5, 0.5" />
        <Setter Property="Text" Value="&#xf110;" />

        <Style.Resources>
            <Storyboard x:Key="Spin">
                <DoubleAnimation
                    RepeatBehavior="Forever"
                    Storyboard.TargetProperty="(UIElement.RenderTransform).(RotateTransform.Angle)"
                    From="0"
                    To="360"
                    Duration="0:0:2" />
            </Storyboard>
        </Style.Resources>

        <Style.Triggers>
            <DataTrigger Binding="{Binding RelativeSource={x:Static RelativeSource.Self}, Path=IsVisible}" Value="True">
                <DataTrigger.EnterActions>
                    <BeginStoryboard Name="SpinStoryboard" Storyboard="{StaticResource Spin}" />
                </DataTrigger.EnterActions>
                <DataTrigger.ExitActions>
                    <RemoveStoryboard BeginStoryboardName="SpinStoryboard" />
                </DataTrigger.ExitActions>
            </DataTrigger>
        </Style.Triggers>

    </Style>

    <Style
        x:Key="TextBlockEditTBName"
        BasedOn="{StaticResource BaseTextBlockStyle}"
        TargetType="{x:Type TextBlock}">
        <Setter Property="FontFamily" Value="{StaticResource FontAwesome}" />
        <Setter Property="FontSize" Value="{StaticResource FontSizeLarge}" />
        <Setter Property="FontSize" Value="{StaticResource FontSizeLarge}" />
        <Setter Property="Foreground" Value="{DynamicResource AccentBaseColorBrush}" />
    </Style>
    <Style
        x:Key="TextBlockTitle"
        BasedOn="{StaticResource BaseTextBlockStyle}"
        TargetType="{x:Type TextBlock}">
        <Setter Property="FontFamily" Value="{StaticResource LatoBold}" />
        <!--
            FontSize="{StaticResource FontSizeXXLarge}"
            FontFamily="{StaticResource LatoBold}"
            Foreground="{StaticResource WordBlueBrush}"
            FontWeight="Bold"
        -->
        <Setter Property="FontSize" Value="{StaticResource FontSizeXXLarge}" />
        <Setter Property="FontWeight" Value="Bold" />
        <Setter Property="Foreground" Value="{DynamicResource AccentBaseColorBrush}" />
    </Style>
    <Style
        x:Key="TextBlockSubTitle"
        BasedOn="{StaticResource BaseTextBlockStyle}"
        TargetType="{x:Type TextBlock}">
        <Setter Property="FontFamily" Value="{StaticResource LatoThin}" />
        <!--
            FontSize="{StaticResource FontSizeXXLarge}"
            FontFamily="{StaticResource LatoBold}"
            Foreground="{StaticResource WordBlueBrush}"
            FontWeight="Bold"
        -->
        <Setter Property="FontSize" Value="{StaticResource FontSizeLarge}" />
        <Setter Property="FontWeight" Value="Bold" />
        <Setter Property="Foreground" Value="{DynamicResource GrayBrush5}" />
        <Setter Property="TextWrapping" Value="WrapWithOverflow" />
    </Style>

    <!--#region Metro-->
    <Style
        x:Key="MetroTextBoxCustom"
        BasedOn="{StaticResource MetroTextBox}"
        TargetType="{x:Type TextBox}">
        <Setter Property="FontSize" Value="{StaticResource FontSizeXXLarge}" />
        <Setter Property="Padding" Value="10" />
    </Style>
    <Style
        x:Key="MetroButtonTextBoxCustomWithClear"
        BasedOn="{StaticResource MetroButtonTextBox}"
        TargetType="{x:Type TextBox}">

        <Setter Property="FontSize" Value="{StaticResource FontSizeXXLarge}" />
        <Setter Property="Margin" Value="0,5,0,5" />
        <!--<Setter Property="FontFamily" Value="{StaticResource LatoRegular}" />-->
        <Setter Property="Padding" Value="10" />
        <!--<Setter Property="BorderBrush" Value="{StaticResource GrayBrush1}" />-->
        <!--<Setter Property="BorderThickness" Value=".5" />-->

        <Setter Property="mm:TextBoxHelper.ClearTextButton" Value="True" />
        <!--<Setter Property="mm:TextBoxHelper.ButtonContentTemplate" />-->


        <!--<Style.Triggers>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Opacity" Value="0.56" />
            </Trigger>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="BorderBrush" Value="{DynamicResource AccentColorBrush2}" />
            </Trigger>
            <Trigger Property="IsKeyboardFocused" Value="True">
                <Setter Property="BorderThickness" Value="0,0,0,1" />
                <Setter Property="BorderBrush" Value="{DynamicResource AccentBaseColorBrush}" />
        -->
        <!--<Setter Property="Effect">
                    <Setter.Value>
                        <DropShadowEffect
                            BlurRadius="30"
                            Direction="280"
                            RenderingBias="Performance"
                            Color="{DynamicResource AccentColor}" />
                    </Setter.Value>
                </Setter>-->
        <!--
            </Trigger>
        </Style.Triggers>-->
    </Style>
    <!--<sys:String x:Key="SearchIcon">M42.5,22A12.5,12.5 0 0,1 55,34.5A12.5,12.5 0 0,1 42.5,47C40.14,47 37.92,46.34 36,45.24L26.97,54.27C25.8,55.44 23.9,55.44 22.73,54.27C21.56,53.1 21.56,51.2 22.73,50.03L31.8,40.96C30.66,39.08 30,36.86 30,34.5A12.5,12.5 0 0,1 42.5,22M42.5,26A8.5,8.5 0 0,0 34,34.5A8.5,8.5 0 0,0 42.5,43A8.5,8.5 0 0,0 51,34.5A8.5,8.5 0 0,0 42.5,26Z</sys:String>-->
    <Style
        x:Key="EzzSearchMetroTextBox"
        BasedOn="{StaticResource SearchMetroTextBox}"
        TargetType="{x:Type TextBox}">
        <!--<TextBox.InputBindings>
                    <KeyBinding
                        Key="Return"
                        Command="{Binding FilterCommand, Mode=OneWay}"
                        CommandParameter="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType=TextBox}, Path=Text, Mode=OneWay}" />
                </TextBox.InputBindings>-->

        <Setter Property="mm:TextBoxHelper.ButtonTemplate">
            <Setter.Value>
                <ControlTemplate>
                    <Grid
                        x:Name="PART_ButtonsGrid"
                        Margin="-10,0,0,0"
                        VerticalAlignment="Stretch"
                        DataContext="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=TextBox}}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition />
                            <ColumnDefinition x:Name="CDSeperator" Width="Auto" />
                            <ColumnDefinition x:Name="CDClearButton" Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <Button
                            x:Name="PART_SearchButton"
                            Grid.Column="0"
                            HorizontalContentAlignment="Stretch"
                            VerticalContentAlignment="Stretch"
                            Command="{Binding (ap:TextBoxHelper.FilterCommand), Mode=OneWay}"
                            IsEnabled="{Binding (ap:TextBoxHelper.FilterButtonIsEnabled)}"
                            Style="{DynamicResource ChromelessButtonStyle}"
                            ToolTip="{Binding (ap:TextBoxHelper.FilterButtonToolTip)}">
                            <Grid>
                                <!--  Custom or default search button Content  -->
                                <Grid
                                    x:Name="PART_SearchIcon"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    Visibility="{Binding Path=(ap:TextBoxHelper.IsFiltering), Mode=OneWay, Converter={conv:VisibilityWhenBoolConverter When=True, Value=Collapsed, Otherwise=Visible}}">

                                    <!--  Custom Search Icon  -->
                                    <ContentControl Content="{Binding Path=(ap:TextBoxHelper.FilterButtonContent)}" Visibility="{Binding Path=(ap:TextBoxHelper.FilterButtonContent), Converter={conv:VisibilityWhenNotNullConverter}}" />
                                    <!--  Content="{Binding (ap:TextBoxHelper.FilterButtonContent), FallbackValue={StaticResource SearchIcon}}"  -->

                                    <ContentControl
                                        Content="M42.5,22A12.5,12.5 0 0,1 55,34.5A12.5,12.5 0 0,1 42.5,47C40.14,47 37.92,46.34 36,45.24L26.97,54.27C25.8,55.44 23.9,55.44 22.73,54.27C21.56,53.1 21.56,51.2 22.73,50.03L31.8,40.96C30.66,39.08 30,36.86 30,34.5A12.5,12.5 0 0,1 42.5,22M42.5,26A8.5,8.5 0 0,0 34,34.5A8.5,8.5 0 0,0 42.5,43A8.5,8.5 0 0,0 51,34.5A8.5,8.5 0 0,0 42.5,26Z"
                                        Style="{DynamicResource PathIconContentControlStyle}"
                                        Visibility="{Binding Path=(ap:TextBoxHelper.FilterButtonContent), Converter={conv:VisibilityWhenNullConverter}}" />
                                    <!--  Visibility="{Binding Path=(ap:TextBoxHelper.IsFiltering), RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=TextBox}, Converter={conv:VisibilityWhenFalseConverter Value=Visible, Otherwise=Collapsed}}">  -->
                                    <!--  Content="{Binding Path=(ap:TextBoxHelper.FilterButtonContent), RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=TextBox}}"  -->


                                </Grid>
                                <!--  VerticalAlignment="{TemplateBinding VerticalContentAlignment}"  -->


                                <Viewbox
                                    x:Name="PART_IsBusyIcon"
                                    Width="20"
                                    MinWidth="5"
                                    MinHeight="5"
                                    HorizontalAlignment="Stretch"
                                    VerticalAlignment="Stretch"
                                    StretchDirection="Both"
                                    Visibility="{Binding Path=(ap:TextBoxHelper.IsFiltering), Mode=OneWay, Converter={conv:VisibilityWhenTrueConverter Value=Visible, Otherwise=Collapsed}}">

                                    <TextBlock Foreground="{DynamicResource AccentBaseColorBrush}" Style="{DynamicResource SpinningText}" />
                                </Viewbox>
                                <!--
                                    Style="{DynamicResource SpinningText}"
                                    Visibility="{TemplateBinding ap:IsBusyProperty.Value,
                                    Converter={conv:VisibilityWhenTrueConverter}}"
                                    Foreground="{TemplateBinding Foreground}"
                                    {TemplateBinding ap:IsBusyProperty.Value,
                                    Converter={conv:VisibilityWhenTrueConverter}}
                                -->
                            </Grid>

                        </Button>
                        <!--  Width="{Binding RelativeSource={RelativeSource AncestorType=TextBox}, Path=(mm:TextBoxHelper.ButtonWidth), Mode=OneWay}"  -->
                        <Button
                            x:Name="PART_ClearButton"
                            Grid.Column="2"
                            Command="{Binding (ap:TextBoxHelper.ClearFilterCommand)}"
                            Content="r"
                            IsEnabled="{Binding (ap:TextBoxHelper.ClearButtonIsEnabled)}"
                            Style="{DynamicResource ChromelessButtonStyle}"
                            ToolTip="{Binding (ap:TextBoxHelper.ClearButtonToolTip)}"
                            Visibility="{Binding Text, Converter={conv:VisibilityWhenStringConverter Value=Collapsed, Otherwise=Visible}}" />
                    </Grid>

                    <ControlTemplate.Triggers>
                        <!--<Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="PART_ButtonsGrid" Property="Background" Value="{DynamicResource WindowBackgroundBrush}" />

                                        </Trigger>-->
                        <DataTrigger Binding="{Binding (ap:TextBoxHelper.RequireFilterAction), RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=TextBox}}" Value="True">
                            <Setter TargetName="PART_SearchButton" Property="ToolTip" Value="{Binding (ap:TextBoxHelper.RequireFilterButtonToolTip), RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=TextBox}}" />
                        </DataTrigger>
                        <!--<Trigger Property="{Binding Text, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=TextBox}}" Value="">
                            <Setter TargetName="PART_SearchIcon" Property="Visibility" Value="Hidden" />
                            <Setter TargetName="PART_IsBusyIcon" Property="Visibility" Value="Visible" />
                        </Trigger>-->

                        <Trigger SourceName="PART_ClearButton" Property="Visibility" Value="Visible">
                            <Setter TargetName="CDClearButton" Property="Width" Value="*" />
                            <!--<Setter TargetName="PART_SearchButton" Property="Grid.ColumnSpan" Value="3" />-->
                            <Setter TargetName="CDSeperator" Property="Width" Value=".1*" />
                            <Setter TargetName="PART_ButtonsGrid" Property="Margin" Value="-30,0,0,0" />
                        </Trigger>

                        <!--<Trigger SourceName="PART_ClearButton" Property="IsMouseOver" Value="False">
                                            <Setter TargetName="PART_ClearButton" Property="Background" Value="{DynamicResource WindowBackgroundBrush}" />
                                        </Trigger>-->
                        <Trigger SourceName="PART_ClearButton" Property="IsMouseOver" Value="True">
                            <Setter TargetName="PART_ClearButton" Property="Background" Value="{DynamicResource GrayBrush8}" />
                            <Setter TargetName="PART_ClearButton" Property="Foreground" Value="{DynamicResource AccentColorBrush}" />
                        </Trigger>
                        <Trigger SourceName="PART_ClearButton" Property="IsPressed" Value="True">
                            <Setter TargetName="PART_ClearButton" Property="Background" Value="{DynamicResource BlackBrush}" />
                            <Setter TargetName="PART_ClearButton" Property="Foreground" Value="{DynamicResource WhiteBrush}" />
                        </Trigger>

                        <!--<Trigger SourceName="PART_SearchButton" Property="IsMouseOver" Value="False">
                                            <Setter TargetName="PART_SearchButton" Property="Background" Value="{DynamicResource WindowBackgroundBrush}" />
                                        </Trigger>-->
                        <Trigger SourceName="PART_SearchButton" Property="IsMouseOver" Value="True">
                            <Setter TargetName="PART_SearchButton" Property="Background" Value="{DynamicResource GrayBrush8}" />
                            <Setter TargetName="PART_SearchButton" Property="Foreground" Value="{DynamicResource AccentColorBrush}" />
                        </Trigger>
                        <Trigger SourceName="PART_SearchButton" Property="IsPressed" Value="True">
                            <Setter TargetName="PART_SearchButton" Property="Background" Value="{DynamicResource BlackBrush}" />
                            <Setter TargetName="PART_SearchButton" Property="Foreground" Value="{DynamicResource WhiteBrush}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>

        </Setter>
        <!--<Setter Property="FlowDirection" Value="{lex:BLoc FlowDirection, TargetNullValue=LeftToRight}" />-->
        <Setter Property="mm:TextBoxHelper.ClearTextButton" Value="False" />
        <Setter Property="mm:TextBoxHelper.UseFloatingWatermark" Value="True" />
        <!--<Setter Property="mm:TextBoxHelper.Watermark" Value="{lex:BLoc HeaderSearch, FallbackValue=Search, TargetNullValue=Search}" />-->

    </Style>

    <Style
        x:Key="EzzFilterTextBoxBaseStyle"
        BasedOn="{StaticResource MetroTextBox}"
        TargetType="{x:Type TextBox}">
        <!--  change SnapsToDevicePixels to True to view a better border and validation error  -->
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type TextBox}">
                    <ControlTemplate.Resources>
                        <Storyboard x:Key="enterGotFocus">
                            <DoubleAnimation
                                Storyboard.TargetName="PART_Message"
                                Storyboard.TargetProperty="Opacity"
                                To=".2"
                                Duration="0:0:0.2" />
                        </Storyboard>
                        <Storyboard x:Key="exitGotFocus">
                            <DoubleAnimation
                                Storyboard.TargetName="PART_Message"
                                Storyboard.TargetProperty="Opacity"
                                Duration="0:0:0.2" />
                        </Storyboard>

                        <Storyboard x:Key="enterHasText">
                            <DoubleAnimation
                                Storyboard.TargetName="PART_Message"
                                Storyboard.TargetProperty="Opacity"
                                From=".2"
                                To="0"
                                Duration="0:0:0.2" />
                        </Storyboard>
                        <Storyboard x:Key="exitHasText">
                            <DoubleAnimation
                                Storyboard.TargetName="PART_Message"
                                Storyboard.TargetProperty="Opacity"
                                Duration="0:0:0.2" />
                        </Storyboard>
                    </ControlTemplate.Resources>
                    <Grid>
                        <AdornerDecorator x:Name="PART_WaitingForDataEffectAdornerDecorator" Visibility="Collapsed">
                            <Border
                                x:Name="PART_WaitingForDataEffectGrid"
                                Background="{TemplateBinding Background}"
                                BorderBrush="Transparent"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Effect="{DynamicResource WaitingForDataEffect}"
                                SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                        </AdornerDecorator>
                        <Border
                            x:Name="Base"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                        <Grid x:Name="PART_InnerGrid" Margin="2">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition x:Name="TextColumn" Width="*" />
                                <ColumnDefinition x:Name="ButtonColumn" Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition x:Name="ButtonRow" Height="*" />
                            </Grid.RowDefinitions>

                            <ScrollViewer
                                x:Name="PART_ContentHost"
                                Grid.Row="1"
                                Grid.Column="0"
                                Margin="2"
                                VerticalAlignment="Stretch"
                                Background="{x:Null}"
                                BorderThickness="0"
                                IsTabStop="False" />
                            <TextBlock
                                x:Name="PART_Message"
                                Grid.Row="1"
                                Grid.Column="0"
                                Margin="6,2,6,2"
                                Padding="{TemplateBinding Padding}"
                                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                Foreground="{TemplateBinding Foreground}"
                                Style="{DynamicResource MahApps.Metro.Styles.MetroWatermarkTextBlock}"
                                Text="{TemplateBinding mm:TextBoxHelper.Watermark}"
                                TextAlignment="{TemplateBinding mm:TextBoxHelper.WatermarkAlignment}"
                                TextTrimming="{TemplateBinding mm:TextBoxHelper.WatermarkTrimming}"
                                TextWrapping="{TemplateBinding mm:TextBoxHelper.WatermarkWrapping}"
                                Visibility="Collapsed" />
                            <ContentControl
                                x:Name="PART_FloatingMessageContainer"
                                Grid.Row="0"
                                Grid.Column="0"
                                Style="{DynamicResource FloatingMessageContainerStyle}">
                                <ContentControl.Height>
                                    <MultiBinding Converter="{mmConverters:MathMultiplyConverter}">
                                        <Binding
                                            ElementName="PART_FloatingMessage"
                                            Mode="OneWay"
                                            Path="ActualHeight" />
                                        <Binding
                                            ElementName="PART_FloatingMessageContainer"
                                            Mode="OneWay"
                                            Path="Opacity" />
                                    </MultiBinding>
                                </ContentControl.Height>
                                <TextBlock
                                    x:Name="PART_FloatingMessage"
                                    HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                    VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                    Foreground="{TemplateBinding Foreground}"
                                    Style="{DynamicResource MetroAutoCollapsingTextBlock}"
                                    Text="{TemplateBinding mm:TextBoxHelper.Watermark}"
                                    TextAlignment="{TemplateBinding mm:TextBoxHelper.WatermarkAlignment}"
                                    TextTrimming="{TemplateBinding mm:TextBoxHelper.WatermarkTrimming}">
                                    <TextBlock.RenderTransform>
                                        <TranslateTransform x:Name="FloatingMessageTransform">
                                            <TranslateTransform.Y>
                                                <MultiBinding Converter="{mmConverters:MathSubtractConverter}">
                                                    <Binding
                                                        ElementName="PART_FloatingMessage"
                                                        Mode="OneWay"
                                                        Path="ActualHeight" />
                                                    <Binding
                                                        ElementName="PART_FloatingMessageContainer"
                                                        Mode="OneWay"
                                                        Path="ActualHeight" />
                                                </MultiBinding>
                                            </TranslateTransform.Y>
                                        </TranslateTransform>
                                    </TextBlock.RenderTransform>
                                </TextBlock>
                            </ContentControl>

                            <!--  Buttons  -->
                            <Grid
                                x:Name="PART_ButtonsGrid"
                                Grid.Row="0"
                                Grid.RowSpan="2"
                                Grid.Column="1">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition x:Name="ButtonsSeperator" Width="3" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>

                                <Button
                                    x:Name="PART_FilterButton"
                                    Grid.Column="0"
                                    Width="{TemplateBinding mm:TextBoxHelper.ButtonWidth}"
                                    Command="{TemplateBinding ap:TextBoxHelper.FilterCommand}"
                                    Content="{TemplateBinding ap:TextBoxHelper.FilterButtonContent}"
                                    Focusable="False"
                                    FontFamily="{TemplateBinding mm:TextBoxHelper.ButtonFontFamily}"
                                    FontSize="{TemplateBinding mm:TextBoxHelper.ButtonFontSize}"
                                    Foreground="{TemplateBinding Foreground}"
                                    IsEnabled="{TemplateBinding ap:TextBoxHelper.FilterButtonIsEnabled}"
                                    IsTabStop="False"
                                    Style="{DynamicResource ChromelessButtonStyle}"
                                    ToolTip="{TemplateBinding ap:TextBoxHelper.FilterButtonToolTip}"
                                    Visibility="{TemplateBinding ap:TextBoxHelper.FilterButtonVisibility}" />

                                <Button
                                    x:Name="PART_ClearText"
                                    Grid.Column="2"
                                    Command="{TemplateBinding ap:TextBoxHelper.ClearFilterCommand}"
                                    Width="{TemplateBinding mm:TextBoxHelper.ButtonWidth}"
                                    mm:TextBoxHelper.IsClearTextButtonBehaviorEnabled="True"
                                    Content="{TemplateBinding mm:TextBoxHelper.ButtonContent}"
                                    ContentTemplate="{TemplateBinding mm:TextBoxHelper.ButtonContentTemplate}"
                                    Focusable="False"
                                    FontFamily="{TemplateBinding mm:TextBoxHelper.ButtonFontFamily}"
                                    FontSize="{TemplateBinding mm:TextBoxHelper.ButtonFontSize}"
                                    Foreground="{TemplateBinding Foreground}"
                                    IsEnabled="{TemplateBinding ap:TextBoxHelper.ClearButtonIsEnabled}"
                                    IsTabStop="False"
                                    Style="{DynamicResource ChromelessButtonStyle}"
                                    Template="{TemplateBinding mm:TextBoxHelper.ButtonTemplate}"
                                    ToolTip="{TemplateBinding ap:TextBoxHelper.ClearButtonToolTip}"
                                    Visibility="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=(mm:TextBoxHelper.HasText), Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}" />
                                <!--  Visibility="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=(mm:TextBoxHelper.TextButton), Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}"  -->
                            </Grid>
                        </Grid>
                        <Border
                            x:Name="DisabledVisualElement"
                            Background="{DynamicResource ControlsDisabledBrush}"
                            BorderBrush="{DynamicResource ControlsDisabledBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            IsHitTestVisible="False"
                            Opacity="0"
                            SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"
                            Visibility="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=(mm:ControlsHelper.DisabledVisualElementVisibility), Mode=OneWay}" />
                    </Grid>
                    <ControlTemplate.Triggers>

                        <DataTrigger Binding="{Binding RelativeSource={RelativeSource Self}, Path=(ap:TextBoxHelper.RequireFilterAction)}" Value="True">
                            <!--<Setter TargetName="PART_FilterButton" Property="Content">
                                <Setter.Value>
                                    <ContentControl
                                            x:Name="PART_PackIcon"
                                            Width="{Binding RelativeSource={RelativeSource Self}, Path=(mm:TextBoxHelper.ButtonWidth), Mode=OneWay}"
                                            Height="{Binding RelativeSource={RelativeSource Self}, Path=(mm:TextBoxHelper.ButtonWidth), Mode=OneWay}"
                                            Padding="3"
                                            HorizontalAlignment="Center"
                                            VerticalAlignment="Center"
                                            Content="M42.5,22A12.5,12.5 0 0,1 55,34.5A12.5,12.5 0 0,1 42.5,47C40.14,47 37.92,46.34 36,45.24L26.97,54.27C25.8,55.44 23.9,55.44 22.73,54.27C21.56,53.1 21.56,51.2 22.73,50.03L31.8,40.96C30.66,39.08 30,36.86 30,34.5A12.5,12.5 0 0,1 42.5,22M42.5,26A8.5,8.5 0 0,0 34,34.5A8.5,8.5 0 0,0 42.5,43A8.5,8.5 0 0,0 51,34.5A8.5,8.5 0 0,0 42.5,26Z"
                                            Style="{DynamicResource PathIconContentControlStyle}" />
                                </Setter.Value>
                            </Setter>-->
                            <!--  RequireFilterButtonToolTip  -->
                            <Setter Property="ap:TextBoxHelper.FilterButtonToolTip" Value="{Binding RelativeSource={RelativeSource Self}, Path=(ap:TextBoxHelper.RequireFilterButtonToolTip)}" />
                        </DataTrigger>
                        <DataTrigger Binding="{Binding RelativeSource={RelativeSource Self}, Path=(ap:TextBoxHelper.IsFiltering)}" Value="True">
                            <Setter TargetName="PART_FilterButton" Property="Content">
                                <Setter.Value>
                                    <Viewbox
                                        x:Name="PART_IsBusyIcon"
                                        Width="20"
                                        MinWidth="5"
                                        MinHeight="5"
                                        HorizontalAlignment="Stretch"
                                        VerticalAlignment="Stretch"
                                        StretchDirection="Both">
                                        <!--  Visibility="{Binding Path=(ap:TextBoxHelper.IsFiltering), Mode=OneWay, Converter={conv:VisibilityWhenTrueConverter Value=Visible, Otherwise=Collapsed}}"  -->
                                        <TextBlock Foreground="{DynamicResource AccentBaseColorBrush}" Style="{DynamicResource SpinningText}" />
                                    </Viewbox>
                                </Setter.Value>
                            </Setter>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding RelativeSource={RelativeSource Self}, Path=(mm:TextBoxHelper.ButtonsAlignment)}" Value="Left">
                            <Setter TargetName="ButtonColumn" Property="Width" Value="*" />
                            <Setter TargetName="PART_ButtonsGrid" Property="Grid.Column" Value="0" />
                            <Setter TargetName="PART_ClearText" Property="Grid.Column" Value="0" />
                            <Setter TargetName="PART_ContentHost" Property="Grid.Column" Value="1" />
                            <Setter TargetName="PART_FilterButton" Property="Grid.Column" Value="2" />
                            <Setter TargetName="PART_Message" Property="Grid.Column" Value="1" />
                            <Setter TargetName="TextColumn" Property="Width" Value="Auto" />
                        </DataTrigger>

                        <DataTrigger Binding="{Binding RelativeSource={RelativeSource Self}, Path=Text}" Value="">
                            <Setter TargetName="PART_Message" Property="Visibility" Value="Visible" />
                        </DataTrigger>

                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding Path=IsVisible, RelativeSource={RelativeSource Self}}" Value="True" />
                                <Condition Binding="{Binding Path=(mm:TextBoxHelper.UseFloatingWatermark), RelativeSource={RelativeSource Self}}" Value="True" />
                                <Condition Binding="{Binding Path=(mm:TextBoxHelper.HasText), RelativeSource={RelativeSource Self}}" Value="True" />
                            </MultiDataTrigger.Conditions>
                            <MultiDataTrigger.EnterActions>
                                <BeginStoryboard Storyboard="{StaticResource ShowFloatingMessageStoryboard}" />
                            </MultiDataTrigger.EnterActions>
                            <MultiDataTrigger.ExitActions>
                                <BeginStoryboard Storyboard="{StaticResource HideFloatingMessageStoryboard}" />
                            </MultiDataTrigger.ExitActions>
                        </MultiDataTrigger>

                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=(mm:TextBoxHelper.ButtonsAlignment)}" Value="Right" />
                                <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=(mm:TextBoxHelper.TextButton)}" Value="False" />
                            </MultiDataTrigger.Conditions>
                            <Setter TargetName="PART_ContentHost" Property="Grid.ColumnSpan" Value="2" />
                            <Setter TargetName="PART_Message" Property="Grid.ColumnSpan" Value="2" />
                        </MultiDataTrigger>

                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Base" Property="BorderBrush" Value="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=(mm:ControlsHelper.MouseOverBorderBrush)}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="DisabledVisualElement" Property="Opacity" Value="0.6" />
                        </Trigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsReadOnly" Value="True" />
                                <Condition Property="mm:TextBoxHelper.ButtonCommand" Value="{x:Null}" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="PART_ClearText" Property="IsEnabled" Value="False" />
                        </MultiTrigger>
                        <Trigger Property="IsFocused" Value="True">
                            <Setter TargetName="Base" Property="BorderBrush" Value="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=(mm:ControlsHelper.FocusBorderBrush)}" />
                            <Setter TargetName="PART_FloatingMessage" Property="Foreground" Value="{DynamicResource AccentColorBrush}" />
                            <Setter TargetName="PART_FloatingMessage" Property="Opacity" Value="1" />
                        </Trigger>

                        <Trigger SourceName="PART_ClearText" Property="IsMouseOver" Value="True">
                            <Setter TargetName="PART_ClearText" Property="Background" Value="{DynamicResource GrayBrush8}" />
                            <Setter TargetName="PART_ClearText" Property="Foreground" Value="{DynamicResource AccentColorBrush}" />
                        </Trigger>
                        <Trigger SourceName="PART_ClearText" Property="IsPressed" Value="True">
                            <Setter TargetName="PART_ClearText" Property="Background" Value="{DynamicResource BlackBrush}" />
                            <Setter TargetName="PART_ClearText" Property="Foreground" Value="{DynamicResource WhiteBrush}" />
                        </Trigger>
                        <Trigger SourceName="PART_FilterButton" Property="IsMouseOver" Value="True">
                            <Setter TargetName="PART_FilterButton" Property="Background" Value="{DynamicResource GrayBrush8}" />
                            <Setter TargetName="PART_FilterButton" Property="Foreground" Value="{DynamicResource AccentColorBrush}" />
                        </Trigger>
                        <Trigger SourceName="PART_FilterButton" Property="IsPressed" Value="True">
                            <Setter TargetName="PART_FilterButton" Property="Background" Value="{DynamicResource BlackBrush}" />
                            <Setter TargetName="PART_FilterButton" Property="Foreground" Value="{DynamicResource WhiteBrush}" />
                        </Trigger>

                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsVisible" Value="True" />
                                <Condition Property="mm:TextBoxHelper.IsWaitingForData" Value="True" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="PART_WaitingForDataEffectAdornerDecorator" Property="Visibility" Value="Visible" />
                            <MultiTrigger.EnterActions>
                                <BeginStoryboard Storyboard="{StaticResource WaitingForDataStoryboard}" />
                            </MultiTrigger.EnterActions>
                        </MultiTrigger>
                        <Trigger Property="mm:TextBoxHelper.IsWaitingForData" Value="False">
                            <Setter TargetName="PART_WaitingForDataEffectAdornerDecorator" Property="Visibility" Value="Collapsed" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="ap:TextBoxHelper.ClearButtonToolTip" Value="{lex:BLoc Clear, TargetNullValue=Clear}" />
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="ap:TextBoxHelper.FilterButtonContent">
            <Setter.Value>
                <!--<iconPacks:PackIconFontAwesome Kind="SyncAltSolid"/>-->
                <!--<DataTemplate>-->
                <ContentControl
                        x:Name="PART_PackIcon"
                        Width="{Binding RelativeSource={RelativeSource Self}, Path=(mm:TextBoxHelper.ButtonWidth), Mode=OneWay}"
                        Height="{Binding RelativeSource={RelativeSource Self}, Path=(mm:TextBoxHelper.ButtonWidth), Mode=OneWay}"
                        Padding="3"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Content="M42.5,22A12.5,12.5 0 0,1 55,34.5A12.5,12.5 0 0,1 42.5,47C40.14,47 37.92,46.34 36,45.24L26.97,54.27C25.8,55.44 23.9,55.44 22.73,54.27C21.56,53.1 21.56,51.2 22.73,50.03L31.8,40.96C30.66,39.08 30,36.86 30,34.5A12.5,12.5 0 0,1 42.5,22M42.5,26A8.5,8.5 0 0,0 34,34.5A8.5,8.5 0 0,0 42.5,43A8.5,8.5 0 0,0 51,34.5A8.5,8.5 0 0,0 42.5,26Z"
                        Style="{DynamicResource PathIconContentControlStyle}" />
                <!--</DataTemplate>-->
            </Setter.Value>
        </Setter>
        <!--<Setter Property="ap:TextBoxHelper.RequireFilterButtonToolTip" Value="{lex:BLoc Refresh, TargetNullValue=Refresh}" />-->
        <Setter Property="ap:TextBoxHelper.FilterButtonToolTip" Value="{lex:BLoc Filter, TargetNullValue=Filter}" />
        <Setter Property="mm:TextBoxHelper.ButtonTemplate" Value="{DynamicResource ChromelessButtonTemplate}" />
        <Setter Property="mm:TextBoxHelper.TextButton" Value="True" />
    </Style>
    <Style
        x:Key="EzzFilterTextBoxDynamicIcon"
        BasedOn="{StaticResource EzzFilterTextBoxBaseStyle}"
        TargetType="TextBox">

        <Setter Property="ap:TextBoxHelper.FilterButtonToolTip" Value="{lex:BLoc Search, TargetNullValue=Search}" />

        <Style.Triggers>
            <Trigger Property="ap:TextBoxHelper.RequireFilterAction" Value="False">
                <Setter Property="ap:TextBoxHelper.FilterButtonContent">
                    <Setter.Value>
                        <iconPacks:PackIconFontAwesome Kind="SyncAltSolid" />
                    </Setter.Value>
                </Setter>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style
        x:Key="PathTextBox"
        BasedOn="{StaticResource EzzFilterTextBoxBaseStyle}"
        TargetType="TextBox">
        <Setter Property="ap:TextBoxHelper.EditType" Value="Path" />
        <Setter Property="ap:TextBoxHelper.FilterButtonContent">
            <Setter.Value>
                <iconPacks:PackIconFontAwesome Kind="SyncAltSolid" />
            </Setter.Value>
        </Setter>
        <Setter Property="ap:TextBoxHelper.FilterButtonToolTip" Value="{lex:BLoc Refresh, TargetNullValue=Refresh}" />

        <Style.Triggers>
            <Trigger Property="ap:TextBoxHelper.RequireFilterAction" Value="True">
                <Setter Property="ap:TextBoxHelper.FilterButtonContent">
                    <Setter.Value>
                        <iconPacks:PackIconFontAwesome Kind="ArrowRightSolid" />
                    </Setter.Value>
                </Setter>
            </Trigger>
        </Style.Triggers>
    </Style>
    <Style
        x:Key="PathTextBox2"
        BasedOn="{StaticResource EzzFilterTextBoxBaseStyle}"
        TargetType="TextBox">
        <Setter Property="ap:TextBoxHelper.EditType" Value="Path" />
        <Setter Property="ap:TextBoxHelper.FilterButtonContent">
            <Setter.Value>
                <iconPacks:PackIconFontAwesome Kind="SyncAltSolid" />
            </Setter.Value>
        </Setter>
        <Setter Property="ap:TextBoxHelper.FilterButtonToolTip" Value="{lex:BLoc Refresh, TargetNullValue=Refresh}" />

        <Style.Triggers>
            <Trigger Property="ap:TextBoxHelper.RequireFilterAction" Value="True">
                <Setter Property="ap:TextBoxHelper.FilterButtonContent">
                    <Setter.Value>
                        <iconPacks:PackIconFontAwesome Kind="ArrowRightSolid" />
                    </Setter.Value>
                </Setter>
            </Trigger>
        </Style.Triggers>
    </Style>


    <Style  x:Key="EzzFilterComboBoxBaseStyle"
        BasedOn="{StaticResource MetroComboBox}"
        TargetType="{x:Type ComboBox}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ComboBox}">
                    <ControlTemplate.Resources>
                        <Storyboard x:Key="enterGotFocus">
                            <DoubleAnimation Storyboard.TargetName="PART_WatermarkMessage"
                                             Storyboard.TargetProperty="Opacity"
                                             To=".2"
                                             Duration="0:0:0.2" />
                        </Storyboard>
                        <Storyboard x:Key="exitGotFocus">
                            <DoubleAnimation Storyboard.TargetName="PART_WatermarkMessage"
                                             Storyboard.TargetProperty="Opacity"
                                             Duration="0:0:0.2" />
                        </Storyboard>

                        <Storyboard x:Key="enterHasText">
                            <DoubleAnimation Storyboard.TargetName="PART_WatermarkMessage"
                                             Storyboard.TargetProperty="Opacity"
                                             From=".2"
                                             To="0"
                                             Duration="0:0:0.2" />
                        </Storyboard>
                        <Storyboard x:Key="exitHasText">
                            <DoubleAnimation Storyboard.TargetName="PART_WatermarkMessage"
                                             Storyboard.TargetProperty="Opacity"
                                             Duration="0:0:0.2" />
                        </Storyboard>
                    </ControlTemplate.Resources>
                    <Grid>
                        <Border x:Name="Border"
                                Grid.ColumnSpan="3"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=(mm:ControlsHelper.CornerRadius), Mode=OneWay}"
                                SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />

                        <Grid Margin="{TemplateBinding BorderThickness}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition x:Name="TextColumn" Width="*" />
                                <ColumnDefinition x:Name="ButtonColumn" Width="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=(mm:TextBoxHelper.ButtonWidth), Mode=OneWay}" />
                                <ColumnDefinition x:Name="ToggleButtonColumn" Width="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=(mm:TextBoxHelper.ButtonWidth), Mode=OneWay}" />
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition x:Name="ButtonRow" Height="*" />
                            </Grid.RowDefinitions>

                            <ToggleButton x:Name="PART_DropDownToggle"
                                          Grid.RowSpan="2"
                                          Grid.ColumnSpan="3"
                                          Margin="0"
                                          Padding="{TemplateBinding Padding}"
                                          VerticalAlignment="Stretch"
                                          mm:ControlsHelper.CornerRadius="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=(mm:ControlsHelper.CornerRadius), Mode=OneWay}"
                                          mm:TextBoxHelper.ButtonContent="{TemplateBinding mm:TextBoxHelper.ButtonContent}"
                                          mm:TextBoxHelper.ButtonContentTemplate="{TemplateBinding mm:TextBoxHelper.ButtonContentTemplate}"
                                          mm:TextBoxHelper.ButtonFontFamily="{TemplateBinding mm:TextBoxHelper.ButtonFontFamily}"
                                          mm:TextBoxHelper.ButtonFontSize="{TemplateBinding mm:TextBoxHelper.ButtonFontSize}"
                                          mm:TextBoxHelper.ButtonWidth="{TemplateBinding mm:TextBoxHelper.ButtonWidth}"
                                          mm:TextBoxHelper.ClearTextButton="{TemplateBinding mm:TextBoxHelper.ClearTextButton}"
                                          Background="Transparent"
                                          BorderThickness="0"
                                          Foreground="{TemplateBinding Foreground}"
                                          IsEnabled="{TemplateBinding ap:TextBoxHelper.IsFiltering, Converter={conv:BooleanInvertConverter}}"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                          KeyboardNavigation.IsTabStop="False"
                                          SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"
                                          Style="{DynamicResource MetroComboBoxDropDownToggleButtonStyle}" />

                            <TextBox x:Name="PART_EditableTextBox"
                                     Grid.Row="1"
                                     Grid.Column="0"
                                     Margin="0"
                                     Padding="{TemplateBinding Padding}"
                                     HorizontalAlignment="Stretch"
                                     VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                     HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                                     mm:TextBoxHelper.ButtonContent="{TemplateBinding mm:TextBoxHelper.ButtonContent}"
                                     mm:TextBoxHelper.ButtonContentTemplate="{TemplateBinding mm:TextBoxHelper.ButtonContentTemplate}"
                                     mm:TextBoxHelper.ButtonFontFamily="{TemplateBinding mm:TextBoxHelper.ButtonFontFamily}"
                                     mm:TextBoxHelper.ButtonFontSize="{TemplateBinding mm:TextBoxHelper.ButtonFontSize}"
                                     mm:TextBoxHelper.ButtonWidth="{TemplateBinding mm:TextBoxHelper.ButtonWidth}"
                                     mm:TextBoxHelper.HasText="{TemplateBinding mm:TextBoxHelper.HasText}"
                                     mm:TextBoxHelper.UseFloatingWatermark="{TemplateBinding mm:TextBoxHelper.UseFloatingWatermark}"
                                     mm:TextBoxHelper.Watermark="{TemplateBinding mm:TextBoxHelper.Watermark}"
                                     mm:TextBoxHelper.WatermarkAlignment="{TemplateBinding mm:TextBoxHelper.WatermarkAlignment}"
                                     mm:TextBoxHelper.WatermarkTrimming="{TemplateBinding mm:TextBoxHelper.WatermarkTrimming}"
                                     ap:TextBoxHelper.IsFiltering="{TemplateBinding ap:TextBoxHelper.IsFiltering}"
                                     ap:TextBoxHelper.AllowToCalculate="{TemplateBinding ap:TextBoxHelper.AllowToCalculate}"
                                     ap:TextBoxHelper.AllowToTogglesTheSign="{TemplateBinding ap:TextBoxHelper.AllowToTogglesTheSign}"
                                     ap:TextBoxHelper.ClearButtonIsEnabled="{TemplateBinding ap:TextBoxHelper.ClearButtonIsEnabled}"
                                     ap:TextBoxHelper.ClearButtonToolTip="{TemplateBinding ap:TextBoxHelper.ClearButtonToolTip}"
                                     ap:TextBoxHelper.ClearFilterCommand="{TemplateBinding ap:TextBoxHelper.ClearFilterCommand}"
                                     ap:TextBoxHelper.EditType="{TemplateBinding ap:TextBoxHelper.EditType}"
                                     ap:TextBoxHelper.EnableEzzTextBox="{TemplateBinding ap:TextBoxHelper.EnableEzzTextBox}"
                                     ap:TextBoxHelper.FilterButtonContent="{TemplateBinding ap:TextBoxHelper.FilterButtonContent}"
                                     ap:TextBoxHelper.FilterButtonContentTemplate="{TemplateBinding ap:TextBoxHelper.FilterButtonContentTemplate}"
                                     ap:TextBoxHelper.FilterButtonIsEnabled="{TemplateBinding ap:TextBoxHelper.FilterButtonIsEnabled}"
                                     ap:TextBoxHelper.FilterButtonTemplate="{TemplateBinding ap:TextBoxHelper.FilterButtonTemplate}"
                                     ap:TextBoxHelper.FilterButtonToolTip="{TemplateBinding ap:TextBoxHelper.FilterButtonToolTip}"
                                     ap:TextBoxHelper.FilterButtonVisibility="{TemplateBinding ap:TextBoxHelper.FilterButtonVisibility}"
                                     ap:TextBoxHelper.FilterByEnterKey="{TemplateBinding ap:TextBoxHelper.FilterByEnterKey}"
                                     ap:TextBoxHelper.FilterCommand="{TemplateBinding ap:TextBoxHelper.FilterCommand}"
                                     ap:TextBoxHelper.FreeInputForCalculating="{TemplateBinding ap:TextBoxHelper.FreeInputForCalculating}"
                                     ap:TextBoxHelper.MaxNumber="{TemplateBinding ap:TextBoxHelper.MaxNumber}"
                                     ap:TextBoxHelper.MinNumber="{TemplateBinding ap:TextBoxHelper.MinNumber}"
                                     ap:TextBoxHelper.NumericType="{TemplateBinding ap:TextBoxHelper.NumericType}"
                                     ap:TextBoxHelper.RequireFilterAction="{TemplateBinding ap:TextBoxHelper.RequireFilterAction}"
                                     ap:TextBoxHelper.RequireFilterButtonToolTip="{TemplateBinding ap:TextBoxHelper.RequireFilterButtonToolTip}"
                                     ap:TextBoxHelper.SelectAllOnFocus="{TemplateBinding ap:TextBoxHelper.SelectAllOnFocus}"
                                     Background="{x:Null}"
                                     BorderThickness="0"
                                     CharacterCasing="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=(mm:ComboBoxHelper.CharacterCasing), Mode=OneWay}"
                                     Focusable="True"
                                     FontFamily="{TemplateBinding FontFamily}"
                                     FontSize="{TemplateBinding FontSize}"
                                     Foreground="{TemplateBinding Foreground}"
                                     IsReadOnly="{TemplateBinding IsReadOnly}"
                                     MaxLength="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=(mm:ComboBoxHelper.MaxLength), Mode=OneWay}"
                                     SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"
                                     Style="{StaticResource EzzFilterTextBoxBaseStyle}"
                                     Visibility="Collapsed" />
                            <!--Text="{TemplateBinding Text}"-->

                            <TextBlock x:Name="PART_WatermarkMessage"
                                       Grid.Row="1"
                                       Grid.Column="0"
                                       Margin="5 0"
                                       Padding="{TemplateBinding Padding}"
                                       HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                       VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                       Foreground="{TemplateBinding Foreground}"
                                       Style="{DynamicResource MahApps.Metro.Styles.MetroWatermarkTextBlock}"
                                       Text="{TemplateBinding mm:TextBoxHelper.Watermark}"
                                       TextAlignment="{TemplateBinding mm:TextBoxHelper.WatermarkAlignment}"
                                       TextTrimming="{TemplateBinding mm:TextBoxHelper.WatermarkTrimming}"
                                       Visibility="Collapsed" />
                            <ContentControl x:Name="PART_FloatingMessageContainer"
                                            Grid.Row="0"
                                            Grid.Column="0"
                                            Style="{DynamicResource FloatingMessageContainerStyle}">
                                <ContentControl.Height>
                                    <MultiBinding Converter="{mmConverters:MathMultiplyConverter}">
                                        <Binding ElementName="PART_FloatingMessage"
                                                 Mode="OneWay"
                                                 Path="ActualHeight" />
                                        <Binding ElementName="PART_FloatingMessageContainer"
                                                 Mode="OneWay"
                                                 Path="Opacity" />
                                    </MultiBinding>
                                </ContentControl.Height>
                                <TextBlock x:Name="PART_FloatingMessage"
                                           Margin="2 2 0 0"
                                           HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                           VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                           Foreground="{TemplateBinding Foreground}"
                                           Style="{DynamicResource MetroAutoCollapsingTextBlock}"
                                           Text="{TemplateBinding mm:TextBoxHelper.Watermark}"
                                           TextAlignment="{TemplateBinding mm:TextBoxHelper.WatermarkAlignment}"
                                           TextTrimming="{TemplateBinding mm:TextBoxHelper.WatermarkTrimming}">
                                    <TextBlock.RenderTransform>
                                        <TranslateTransform x:Name="FloatingMessageTransform">
                                            <TranslateTransform.Y>
                                                <MultiBinding Converter="{mmConverters:MathSubtractConverter}">
                                                    <Binding ElementName="PART_FloatingMessage"
                                                             Mode="OneWay"
                                                             Path="ActualHeight" />
                                                    <Binding ElementName="PART_FloatingMessageContainer"
                                                             Mode="OneWay"
                                                             Path="ActualHeight" />
                                                </MultiBinding>
                                            </TranslateTransform.Y>
                                        </TranslateTransform>
                                    </TextBlock.RenderTransform>
                                </TextBlock>
                            </ContentControl>

                            <Grid x:Name="ContentSite"
                                  Grid.Row="1"
                                  Grid.Column="0"
                                  Margin="4 0">
                                <ContentPresenter Margin="{TemplateBinding Padding}"
                                                  HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                  VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                                  Content="{TemplateBinding SelectionBoxItem}"
                                                  ContentStringFormat="{TemplateBinding SelectionBoxItemStringFormat}"
                                                  ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                  ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                                  IsHitTestVisible="False"
                                                  SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                            </Grid>
                        </Grid>

                        <Border x:Name="DisabledVisualElement"
                                Grid.ColumnSpan="3"
                                Background="{DynamicResource ControlsDisabledBrush}"
                                BorderBrush="{DynamicResource ControlsDisabledBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=(mm:ControlsHelper.CornerRadius), Mode=OneWay}"
                                IsHitTestVisible="False"
                                Opacity="0.6"
                                SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"
                                Visibility="Collapsed" />

                        <!--  AllowsTransparency="true" fixes the redraw problem under windows vista/7 with a selected non aero theme  -->
                        <Popup x:Name="PART_Popup"
                               AllowsTransparency="True"
                               Focusable="False"
                               IsOpen="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}}"
                               Placement="Bottom"
                               PopupAnimation="{DynamicResource {x:Static SystemParameters.ComboBoxPopupAnimationKey}}">
                            <Grid MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}" MaxHeight="{Binding MaxDropDownHeight, RelativeSource={RelativeSource TemplatedParent}}">
                                <Border x:Name="PopupBorder"
                                        Height="Auto"
                                        HorizontalAlignment="Stretch"
                                        Background="{DynamicResource WhiteBrush}"
                                        BorderBrush="{DynamicResource ComboBoxPopupBorderBrush}"
                                        BorderThickness="{DynamicResource ComboBoxPopupBorderThemeThickness}"
                                        SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}">
                                    <ScrollViewer Padding="{DynamicResource ComboBoxPopupBorderThemePadding}" BorderThickness="0">
                                        <ItemsPresenter x:Name="ItemsPresenter" KeyboardNavigation.DirectionalNavigation="Contained" />
                                    </ScrollViewer>
                                </Border>
                            </Grid>
                        </Popup>
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="FocusStates">
                                <VisualState x:Name="Focused" />
                                <VisualState x:Name="Unfocused" />
                                <VisualState x:Name="FocusedDropDown">
                                    <Storyboard>
                                        <ObjectAnimationUsingKeyFrames Storyboard.TargetName="PopupBorder"
                                                                       Storyboard.TargetProperty="(UIElement.Visibility)"
                                                                       Duration="00:00:00">
                                            <DiscreteObjectKeyFrame KeyTime="00:00:00">
                                                <DiscreteObjectKeyFrame.Value>
                                                    <Visibility>Visible</Visibility>
                                                </DiscreteObjectKeyFrame.Value>
                                            </DiscreteObjectKeyFrame>
                                        </ObjectAnimationUsingKeyFrames>
                                    </Storyboard>
                                </VisualState>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=IsEditable}" Value="True" />
                                <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=ContextMenu, Converter={x:Static mmConverters:IsNullConverter.Instance}}" Value="False" />
                            </MultiDataTrigger.Conditions>
                            <Setter TargetName="PART_EditableTextBox" Property="ContextMenu" Value="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=ContextMenu}" />
                        </MultiDataTrigger>

                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=IsEditable}" Value="False" />
                                <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=SelectedItem}" Value="{x:Null}" />
                            </MultiDataTrigger.Conditions>
                            <Setter TargetName="PART_WatermarkMessage" Property="Visibility" Value="Visible" />
                        </MultiDataTrigger>
                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=SelectedItem, Mode=OneWay, Converter={x:Static mmConverters:IsNullConverter.Instance}}" Value="True" />
                                <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=IsKeyboardFocusWithin}" Value="True" />
                                <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=IsEditable}" Value="False" />
                            </MultiDataTrigger.Conditions>
                            <MultiDataTrigger.EnterActions>
                                <BeginStoryboard Storyboard="{StaticResource enterGotFocus}" />
                            </MultiDataTrigger.EnterActions>
                            <MultiDataTrigger.ExitActions>
                                <BeginStoryboard Storyboard="{StaticResource exitGotFocus}" />
                            </MultiDataTrigger.ExitActions>
                        </MultiDataTrigger>
                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=SelectedItem, Mode=OneWay, Converter={x:Static mmConverters:IsNullConverter.Instance}}" Value="False" />
                                <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=IsEditable}" Value="False" />
                            </MultiDataTrigger.Conditions>
                            <MultiDataTrigger.EnterActions>
                                <BeginStoryboard Storyboard="{StaticResource enterHasText}" />
                            </MultiDataTrigger.EnterActions>
                            <MultiDataTrigger.ExitActions>
                                <BeginStoryboard Storyboard="{StaticResource exitHasText}" />
                            </MultiDataTrigger.ExitActions>
                        </MultiDataTrigger>
                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding Path=IsVisible, RelativeSource={RelativeSource Self}}" Value="True" />
                                <Condition Binding="{Binding Path=(mm:TextBoxHelper.UseFloatingWatermark), RelativeSource={RelativeSource Self}}" Value="True" />
                                <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=IsEditable}" Value="False" />
                                <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=SelectedItem, Mode=OneWay, Converter={x:Static mmConverters:IsNullConverter.Instance}}" Value="False" />
                            </MultiDataTrigger.Conditions>
                            <MultiDataTrigger.EnterActions>
                                <BeginStoryboard Storyboard="{StaticResource ShowFloatingMessageStoryboard}" />
                            </MultiDataTrigger.EnterActions>
                            <MultiDataTrigger.ExitActions>
                                <BeginStoryboard Storyboard="{StaticResource HideFloatingMessageStoryboard}" />
                            </MultiDataTrigger.ExitActions>
                        </MultiDataTrigger>

                        <DataTrigger Binding="{Binding RelativeSource={RelativeSource Self}, Path=(mm:TextBoxHelper.ClearTextButton)}" Value="False">
                            <Setter TargetName="ContentSite" Property="Grid.ColumnSpan" Value="2" />
                            <Setter TargetName="PART_EditableTextBox" Property="Grid.ColumnSpan" Value="2" />
                            <Setter TargetName="PART_WatermarkMessage" Property="Grid.ColumnSpan" Value="2" />
                        </DataTrigger>
                        <Trigger Property="IsEditable" Value="True">
                            <Setter Property="IsTabStop" Value="false" />
                            <Setter TargetName="ContentSite" Property="Visibility" Value="Hidden" />
                            <Setter TargetName="PART_DropDownToggle" Property="Background" Value="{x:Null}" />
                            <Setter TargetName="PART_DropDownToggle" Property="Focusable" Value="False" />
                            <Setter TargetName="PART_EditableTextBox" Property="Visibility" Value="Visible" />
                        </Trigger>

                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsEditable" Value="False" />
                                <Condition Property="IsMouseOver" Value="True" />
                            </MultiTrigger.Conditions>
                            <Setter Property="Background" Value="{DynamicResource GrayBrush9}" />
                        </MultiTrigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Border" Property="BorderBrush" Value="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=(mm:ControlsHelper.MouseOverBorderBrush)}" />
                        </Trigger>

                        <Trigger SourceName="PART_EditableTextBox" Property="IsFocused" Value="True">
                            <Setter TargetName="Border" Property="BorderBrush" Value="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=(mm:ControlsHelper.FocusBorderBrush)}" />
                        </Trigger>
                        <Trigger Property="IsFocused" Value="True">
                            <Setter TargetName="Border" Property="BorderBrush" Value="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=(mm:ControlsHelper.FocusBorderBrush)}" />
                            <Setter TargetName="PART_FloatingMessage" Property="Foreground" Value="{DynamicResource AccentColorBrush}" />
                            <Setter TargetName="PART_FloatingMessage" Property="Opacity" Value="1" />
                        </Trigger>
                        <Trigger Property="IsKeyboardFocusWithin" Value="True">
                            <Setter TargetName="Border" Property="BorderBrush" Value="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=(mm:ControlsHelper.FocusBorderBrush)}" />
                        </Trigger>

                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="DisabledVisualElement" Property="Visibility" Value="Visible" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>

            </Setter.Value>
        </Setter>
        <Setter Property="IsEditable" Value="True"/>
    </Style>
    <Style
        x:Key="EzzFilterComboBoxDynamicIcon"
        BasedOn="{StaticResource EzzFilterComboBoxBaseStyle}"
        TargetType="ComboBox">

        <Setter Property="ap:TextBoxHelper.FilterButtonToolTip" Value="{lex:BLoc Search, TargetNullValue=Search}" />

        <Style.Triggers>
            <Trigger Property="ap:TextBoxHelper.RequireFilterAction" Value="False">
                <Setter Property="ap:TextBoxHelper.FilterButtonContent">
                    <Setter.Value>
                        <iconPacks:PackIconFontAwesome Kind="SyncAltSolid" />
                    </Setter.Value>
                </Setter>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style
        x:Key="PathComboBox"
        BasedOn="{StaticResource EzzFilterComboBoxBaseStyle}"
        TargetType="ComboBox">
        <Setter Property="ap:TextBoxHelper.EditType" Value="Path" />
        <Setter Property="ap:TextBoxHelper.FilterButtonContent">
            <Setter.Value>
                <iconPacks:PackIconFontAwesome Kind="SyncAltSolid" />
            </Setter.Value>
        </Setter>
        <Setter Property="ap:TextBoxHelper.FilterButtonToolTip" Value="{lex:BLoc Refresh, TargetNullValue=Refresh}" />

        <Style.Triggers>
            <Trigger Property="ap:TextBoxHelper.RequireFilterAction" Value="True">
                <Setter Property="ap:TextBoxHelper.FilterButtonContent">
                    <Setter.Value>
                        <iconPacks:PackIconFontAwesome Kind="ArrowRightSolid" />
                    </Setter.Value>
                </Setter>
            </Trigger>
        </Style.Triggers>
    </Style>
    <Style
        x:Key="PathComboBox2"
        BasedOn="{StaticResource EzzFilterComboBoxBaseStyle}"
        TargetType="ComboBox">
        <Setter Property="ap:TextBoxHelper.EditType" Value="Path" />
        <Setter Property="ap:TextBoxHelper.FilterButtonContent">
            <Setter.Value>
                <iconPacks:PackIconFontAwesome Kind="SyncAltSolid" />
            </Setter.Value>
        </Setter>
        <Setter Property="ap:TextBoxHelper.FilterButtonToolTip" Value="{lex:BLoc Refresh, TargetNullValue=Refresh}" />

        <Style.Triggers>
            <Trigger Property="ap:TextBoxHelper.RequireFilterAction" Value="True">
                <Setter Property="ap:TextBoxHelper.FilterButtonContent">
                    <Setter.Value>
                        <iconPacks:PackIconFontAwesome Kind="ArrowRightSolid" />
                    </Setter.Value>
                </Setter>
            </Trigger>
        </Style.Triggers>
    </Style>
    <!--#endregion Metro-->

</ResourceDictionary>