<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ap="clr-namespace:FolderIconChangerWPF.AttachedProperties"
    xmlns:conv="clr-namespace:FolderIconChangerWPF.ValueConverters"
    xmlns:lex="http://wpflocalizeextension.codeplex.com"
    xmlns:mm="http://metro.mahapps.com/winfx/xaml/controls"
    xmlns:mmConverters="http://metro.mahapps.com/winfx/xaml/shared">

    <Style x:Key="BaseTextBlockDarkStyle" TargetType="{x:Type TextBlock}">
        <Setter Property="FontFamily" Value="{StaticResource LatoRegular}" />
        <Setter Property="FontSize" Value="{StaticResource FontSizeRegular}" />
        <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
        <!--<Setter Property="Background" Value="{StaticResource GrayBrush10}" />-->
    </Style>
    <Style x:Key="BaseTextBoxStyle" TargetType="{x:Type TextBox}">
        <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
        <Setter Property="Background" Value="{DynamicResource GrayBrush8}" />
    </Style>
    <Style
        x:Key="HeaderText"
        BasedOn="{StaticResource BaseTextBlockStyle}"
        TargetType="{x:Type TextBlock}">
        <Setter Property="Foreground" Value="{DynamicResource GrayBrush2}" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="HorizontalAlignment" Value="Center" />
        <Setter Property="Margin" Value="0,6" />
    </Style>
    <Style
        x:Key="TextBoxMedium"
        BasedOn="{StaticResource BaseTextBoxStyle}"
        TargetType="{x:Type TextBox}">
        <Setter Property="FontSize" Value="{StaticResource FontSizeXXLarge}" />
        <Setter Property="FontFamily" Value="{StaticResource LatoRegular}" />
        <Setter Property="Padding" Value="10" />
        <Setter Property="Margin" Value="0,5,0,5" />
        <Setter Property="BorderBrush" Value="{DynamicResource AccentBaseColorBrush}" />
        <Setter Property="BorderThickness" Value="0,0,0,1" />

        <!--<Setter Property="Background" Value="{StaticResource GrayBrush8}" />
        <Setter Property="Foreground" Value="{StaticResource TextBrush}" />-->
        <!--<Setter Property="IsEnabled" Value="{TemplateBinding ap:IsBusyProperty.Value, Converter={conv:BooleanToVisiblityConverter}}"/>-->

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type TextBoxBase}">
                    <Grid>
                        <Border
                            x:Name="border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            SnapsToDevicePixels="True">
                            <ScrollViewer
                                x:Name="PART_ContentHost"
                                Focusable="False"
                                HorizontalScrollBarVisibility="Hidden"
                                VerticalScrollBarVisibility="Hidden" />
                        </Border>
                    </Grid>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="border" Property="Opacity" Value="0.56" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource AccentColorBrush}" />
                        </Trigger>
                        <Trigger Property="IsKeyboardFocused" Value="True">
                            <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource AccentColorBrush}" />
                            <Setter Property="Effect">
                                <Setter.Value>
                                    <DropShadowEffect
                                        BlurRadius="30"
                                        Direction="280"
                                        RenderingBias="Performance"
                                        Color="{DynamicResource AccentColor}" />
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style
        x:Key="TextBoxWithHintLarge"
        BasedOn="{StaticResource BaseTextBoxStyle}"
        TargetType="{x:Type TextBox}">

        <Setter Property="FontSize" Value="{StaticResource FontSizeXXLarge}" />
        <Setter Property="FontFamily" Value="{StaticResource LatoRegular}" />
        <Setter Property="Padding" Value="10" />
        <Setter Property="Margin" Value="0,5,0,5" />
        <Setter Property="BorderBrush" Value="{DynamicResource AccentBaseColorBrush}" />
        <Setter Property="BorderThickness" Value="0,0,0,1" />

        <!--<Setter Property="Background" Value="{StaticResource ForegroundLightBrush}" />
        <Setter Property="Foreground" Value="{StaticResource ForegroundVeryDarkBrush}" />-->
        <!--<Setter Property="IsEnabled" Value="{TemplateBinding ap:IsBusyProperty.Value, Converter={conv:BooleanToVisiblityConverter}}"/>-->

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type TextBoxBase}">

                    <Grid>

                        <Border
                            x:Name="border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            SnapsToDevicePixels="True">
                            <ScrollViewer
                                x:Name="PART_ContentHost"
                                Focusable="False"
                                HorizontalScrollBarVisibility="Hidden"
                                VerticalScrollBarVisibility="Hidden" />
                        </Border>

                        <TextBlock
                            x:Name="placeholder"
                            Padding="{TemplateBinding Padding}"
                            HorizontalAlignment="{TemplateBinding HorizontalAlignment}"
                            VerticalAlignment="Center"
                            FontFamily="{StaticResource LatoThin}"
                            Foreground="{DynamicResource GrayBrush5}"
                            IsHitTestVisible="False"
                            Text="{TemplateBinding Tag}">
                            <TextBlock.Style>
                                <Style TargetType="{x:Type TextBlock}">
                                    <Setter Property="Visibility" Value="Hidden" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Text, RelativeSource={RelativeSource TemplatedParent}}" Value="">
                                            <Setter Property="Visibility" Value="Visible" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>

                        </TextBlock>

                    </Grid>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="border" Property="Opacity" Value="0.56" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource AccentColorBrush}" />
                        </Trigger>
                        <Trigger Property="IsKeyboardFocused" Value="True">
                            <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource AccentColorBrush}" />
                            <Setter Property="Effect">
                                <Setter.Value>
                                    <DropShadowEffect
                                        BlurRadius="30"
                                        Direction="280"
                                        RenderingBias="Performance"
                                        Color="{DynamicResource AccentColor}" />
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>

    </Style>

    <Style x:Key="PasswordBoxWithHintLarge" TargetType="{x:Type PasswordBox}">

        <Setter Property="FontSize" Value="{StaticResource FontSizeXXLarge}" />
        <Setter Property="FontFamily" Value="{StaticResource LatoRegular}" />
        <Setter Property="Padding" Value="10" />
        <Setter Property="Margin" Value="0,5,0,5" />
        <Setter Property="BorderBrush" Value="{StaticResource AccentBaseColorBrush}" />
        <Setter Property="BorderThickness" Value="0,0,0,1" />

        <!--<Setter Property="Background" Value="{StaticResource ForegroundLightBrush}" />
        <Setter Property="Foreground" Value="{StaticResource ForegroundVeryDarkBrush}" />-->
        <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
        <Setter Property="Background" Value="{DynamicResource GrayBrush8}" />

        <Setter Property="ap:MonitorPasswordProperty.Value" Value="True" />
        <!--<Setter Property="IsEnabled" Value="{TemplateBinding ap:IsBusyProperty.Value, Converter={conv:BooleanToVisiblityConverter}}"/>-->

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type PasswordBox}">

                    <Grid>

                        <Border
                            x:Name="border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            SnapsToDevicePixels="True">
                            <ScrollViewer
                                x:Name="PART_ContentHost"
                                Focusable="False"
                                HorizontalScrollBarVisibility="Hidden"
                                VerticalScrollBarVisibility="Hidden" />
                        </Border>

                        <TextBlock
                            x:Name="placeholder"
                            Padding="{TemplateBinding Padding}"
                            HorizontalAlignment="{TemplateBinding HorizontalAlignment}"
                            VerticalAlignment="Center"
                            FontFamily="{StaticResource LatoThin}"
                            FontSize="{TemplateBinding FontSize}"
                            Foreground="{StaticResource GrayBrush5}"
                            IsHitTestVisible="False"
                            Text="{TemplateBinding Tag}"
                            Visibility="{TemplateBinding ap:HasTextProperty.Value,
                                                         Converter={conv:VisibilityWhenFalseConverter}}" />

                    </Grid>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="border" Property="Opacity" Value="0.56" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource AccentColorBrush}" />
                        </Trigger>
                        <Trigger Property="IsKeyboardFocused" Value="True">
                            <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource AccentColorBrush}" />
                            <Setter Property="Effect">
                                <Setter.Value>
                                    <DropShadowEffect
                                        BlurRadius="30"
                                        Direction="280"
                                        RenderingBias="Performance"
                                        Color="{DynamicResource AccentColor}" />
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>

    </Style>

    <Style
        x:Key="SpinningText"
        BasedOn="{StaticResource BaseTextBlockStyle}"
        TargetType="{x:Type TextBlock}">

        <Setter Property="FontFamily" Value="{StaticResource FontAwesome}" />
        <Setter Property="Text" Value="&#xf110;" />
        <Setter Property="RenderTransformOrigin" Value="0.5, 0.5" />
        <Setter Property="RenderTransform">
            <Setter.Value>
                <RotateTransform />
            </Setter.Value>
        </Setter>

        <Style.Resources>
            <Storyboard x:Key="Spin">
                <DoubleAnimation
                    RepeatBehavior="Forever"
                    Storyboard.TargetProperty="(UIElement.RenderTransform).(RotateTransform.Angle)"
                    From="0"
                    To="360"
                    Duration="0:0:2" />
            </Storyboard>
        </Style.Resources>

        <Style.Triggers>
            <DataTrigger Binding="{Binding RelativeSource={x:Static RelativeSource.Self}, Path=IsVisible}" Value="True">
                <DataTrigger.EnterActions>
                    <BeginStoryboard Name="SpinStoryboard" Storyboard="{StaticResource Spin}" />
                </DataTrigger.EnterActions>
                <DataTrigger.ExitActions>
                    <RemoveStoryboard BeginStoryboardName="SpinStoryboard" />
                </DataTrigger.ExitActions>
            </DataTrigger>
        </Style.Triggers>

    </Style>

    <Style
        x:Key="TextBlockEditTBName"
        BasedOn="{StaticResource BaseTextBlockStyle}"
        TargetType="{x:Type TextBlock}">
        <Setter Property="FontSize" Value="{StaticResource FontSizeLarge}" />
        <Setter Property="FontFamily" Value="{StaticResource FontAwesome}" />
        <Setter Property="FontSize" Value="{StaticResource FontSizeLarge}" />
        <Setter Property="Foreground" Value="{DynamicResource AccentBaseColorBrush}" />
    </Style>
    <Style
        x:Key="TextBlockTitle"
        BasedOn="{StaticResource BaseTextBlockStyle}"
        TargetType="{x:Type TextBlock}">
        <!--
            FontSize="{StaticResource FontSizeXXLarge}"
            FontFamily="{StaticResource LatoBold}"
            Foreground="{StaticResource WordBlueBrush}"
            FontWeight="Bold"
        -->
        <Setter Property="FontSize" Value="{StaticResource FontSizeXXLarge}" />
        <Setter Property="FontFamily" Value="{StaticResource LatoBold}" />
        <Setter Property="Foreground" Value="{DynamicResource AccentBaseColorBrush}" />
        <Setter Property="FontWeight" Value="Bold" />
    </Style>
    <Style
        x:Key="TextBlockSubTitle"
        BasedOn="{StaticResource BaseTextBlockStyle}"
        TargetType="{x:Type TextBlock}">
        <!--
            FontSize="{StaticResource FontSizeXXLarge}"
            FontFamily="{StaticResource LatoBold}"
            Foreground="{StaticResource WordBlueBrush}"
            FontWeight="Bold"
        -->
        <Setter Property="FontSize" Value="{StaticResource FontSizeLarge}" />
        <Setter Property="FontFamily" Value="{StaticResource LatoThin}" />
        <Setter Property="Foreground" Value="{DynamicResource GrayBrush5}" />
        <Setter Property="FontWeight" Value="Bold" />
        <Setter Property="TextWrapping" Value="WrapWithOverflow" />
    </Style>

    <!--#region Metro-->
    <Style
        x:Key="MetroTextBoxCustom"
        BasedOn="{StaticResource MetroTextBox}"
        TargetType="{x:Type TextBox}">
        <Setter Property="FontSize" Value="{StaticResource FontSizeXXLarge}" />
        <Setter Property="Padding" Value="10" />
    </Style>
    <Style
        x:Key="MetroButtonTextBoxCustomWithClear"
        BasedOn="{StaticResource MetroButtonTextBox}"
        TargetType="{x:Type TextBox}">

        <Setter Property="FontSize" Value="{StaticResource FontSizeXXLarge}" />
        <!--<Setter Property="FontFamily" Value="{StaticResource LatoRegular}" />-->
        <Setter Property="Padding" Value="10" />
        <Setter Property="Margin" Value="0,5,0,5" />
        <!--<Setter Property="BorderBrush" Value="{StaticResource GrayBrush1}" />-->
        <!--<Setter Property="BorderThickness" Value=".5" />-->

        <Setter Property="mm:TextBoxHelper.ClearTextButton" Value="True" />
        <!--<Setter Property="mm:TextBoxHelper.ButtonContentTemplate" />-->


        <!--<Style.Triggers>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Opacity" Value="0.56" />
            </Trigger>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="BorderBrush" Value="{DynamicResource AccentColorBrush2}" />
            </Trigger>
            <Trigger Property="IsKeyboardFocused" Value="True">
                <Setter Property="BorderThickness" Value="0,0,0,1" />
                <Setter Property="BorderBrush" Value="{DynamicResource AccentBaseColorBrush}" />
        -->
        <!--<Setter Property="Effect">
                    <Setter.Value>
                        <DropShadowEffect
                            BlurRadius="30"
                            Direction="280"
                            RenderingBias="Performance"
                            Color="{DynamicResource AccentColor}" />
                    </Setter.Value>
                </Setter>-->
        <!--
            </Trigger>
        </Style.Triggers>-->
    </Style>



    <Style
        x:Key="EzzSearchMetroTextBox"
        BasedOn="{StaticResource SearchMetroTextBox}"
        TargetType="{x:Type TextBox}">
        <!--<Setter Property="FlowDirection" Value="{lex:BLoc FlowDirection, TargetNullValue=LeftToRight}" />-->
        <Setter Property="mm:TextBoxHelper.ClearTextButton" Value="False" />
        <Setter Property="mm:TextBoxHelper.UseFloatingWatermark" Value="True" />
        <Setter Property="mm:TextBoxHelper.Watermark" Value="{lex:BLoc HeaderSearch, FallbackValue=Search, TargetNullValue=Search}" />
        <!--<TextBox.InputBindings>
                    <KeyBinding
                        Key="Return"
                        Command="{Binding FilterCommand, Mode=OneWay}"
                        CommandParameter="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType=TextBox}, Path=Text, Mode=OneWay}" />
                </TextBox.InputBindings>-->

        <Setter Property="mm:TextBoxHelper.ButtonTemplate">
            <Setter.Value>
                <ControlTemplate>
                    <Grid
                        x:Name="PART_ButtonsGrid"
                        Margin="-10,0,0,0"
                        VerticalAlignment="Stretch">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition />
                            <ColumnDefinition x:Name="CDSeperator" Width="Auto" />
                            <ColumnDefinition x:Name="CDClearButton" Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <Button
                            x:Name="PART_SearchButton"
                            Grid.Column="0"
                            HorizontalContentAlignment="Stretch"
                            VerticalContentAlignment="Stretch"
                            Command="{Binding (ap:TextBoxHelper.FilterCommand), RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=TextBox}, Mode=OneWay}"
                            Style="{DynamicResource ChromelessButtonStyle}">
                            <Grid>
                                <ContentControl
                                    x:Name="PART_SearchIcon"
                                    HorizontalAlignment="Stretch"
                                    VerticalAlignment="Stretch"
                                    Content="M42.5,22A12.5,12.5 0 0,1 55,34.5A12.5,12.5 0 0,1 42.5,47C40.14,47 37.92,46.34 36,45.24L26.97,54.27C25.8,55.44 23.9,55.44 22.73,54.27C21.56,53.1 21.56,51.2 22.73,50.03L31.8,40.96C30.66,39.08 30,36.86 30,34.5A12.5,12.5 0 0,1 42.5,22M42.5,26A8.5,8.5 0 0,0 34,34.5A8.5,8.5 0 0,0 42.5,43A8.5,8.5 0 0,0 51,34.5A8.5,8.5 0 0,0 42.5,26Z"
                                    Style="{DynamicResource PathIconContentControlStyle}"
                                    Visibility="{Binding Path=(ap:TextBoxHelper.IsFiltering), RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=TextBox}, Converter={conv:VisibilityWhenFalseConverter Value=Visible, Otherwise=Collapsed}}">
                                    <!--
                                        Visibility="{TemplateBinding ap:IsBusyProperty.Value,
                                        Converter={conv:VisibilityWhenFalseConverter}}"
                                        {TemplateBinding ap:IsBusyProperty.Value,
                                        Converter={conv:VisibilityWhenFalseConverter}}
                                    -->
                                </ContentControl>
                                <!--  VerticalAlignment="{TemplateBinding VerticalContentAlignment}"  -->


                                <Viewbox
                                    x:Name="PART_IsBusyIcon"
                                    Width="20"
                                    MinWidth="5"
                                    MinHeight="5"
                                    HorizontalAlignment="Stretch"
                                    VerticalAlignment="Stretch"
                                    StretchDirection="Both"
                                    Visibility="{Binding Path=(ap:TextBoxHelper.IsFiltering), RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=TextBox}, Converter={conv:VisibilityWhenTrueConverter Value=Visible, Otherwise=Collapsed}}">

                                    <TextBlock Foreground="{DynamicResource AccentBaseColorBrush}" Style="{DynamicResource SpinningText}" />
                                </Viewbox>
                                <!--
                                    Style="{DynamicResource SpinningText}"
                                    Visibility="{TemplateBinding ap:IsBusyProperty.Value,
                                    Converter={conv:VisibilityWhenTrueConverter}}"
                                    Foreground="{TemplateBinding Foreground}"
                                    {TemplateBinding ap:IsBusyProperty.Value,
                                    Converter={conv:VisibilityWhenTrueConverter}}
                                -->
                            </Grid>

                        </Button>
                        <!--  Width="{Binding RelativeSource={RelativeSource AncestorType=TextBox}, Path=(mm:TextBoxHelper.ButtonWidth), Mode=OneWay}"  -->
                        <Button
                            x:Name="PART_ClearButton"
                            Grid.Column="2"
                            Command="{Binding (ap:TextBoxHelper.ClearFilterCommand), RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=TextBox}}"
                            Content="r"
                            Style="{DynamicResource ChromelessButtonStyle}"
                            Visibility="{Binding Text, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=TextBox}, Converter={conv:VisibilityWhenStringConverter Value=Collapsed, Otherwise=Visible}}" />
                    </Grid>

                    <ControlTemplate.Triggers>
                        <!--<Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="PART_ButtonsGrid" Property="Background" Value="{DynamicResource WindowBackgroundBrush}" />

                                        </Trigger>-->

                        <!--<Trigger Property="{Binding Text, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=TextBox}}" Value="">
                            <Setter TargetName="PART_SearchIcon" Property="Visibility" Value="Hidden" />
                            <Setter TargetName="PART_IsBusyIcon" Property="Visibility" Value="Visible" />
                        </Trigger>-->

                        <Trigger SourceName="PART_ClearButton" Property="Visibility" Value="Visible">
                            <Setter TargetName="PART_ButtonsGrid" Property="Margin" Value="-30,0,0,0" />
                            <!--<Setter TargetName="PART_SearchButton" Property="Grid.ColumnSpan" Value="3" />-->
                            <Setter TargetName="CDSeperator" Property="Width" Value=".1*" />
                            <Setter TargetName="CDClearButton" Property="Width" Value="*" />
                        </Trigger>

                        <!--<Trigger SourceName="PART_ClearButton" Property="IsMouseOver" Value="False">
                                            <Setter TargetName="PART_ClearButton" Property="Background" Value="{DynamicResource WindowBackgroundBrush}" />
                                        </Trigger>-->
                        <Trigger SourceName="PART_ClearButton" Property="IsMouseOver" Value="True">
                            <Setter TargetName="PART_ClearButton" Property="Background" Value="{DynamicResource GrayBrush8}" />
                            <Setter TargetName="PART_ClearButton" Property="Foreground" Value="{DynamicResource AccentColorBrush}" />
                        </Trigger>
                        <Trigger SourceName="PART_ClearButton" Property="IsPressed" Value="True">
                            <Setter TargetName="PART_ClearButton" Property="Background" Value="{DynamicResource BlackBrush}" />
                            <Setter TargetName="PART_ClearButton" Property="Foreground" Value="{DynamicResource WhiteBrush}" />
                        </Trigger>

                        <!--<Trigger SourceName="PART_SearchButton" Property="IsMouseOver" Value="False">
                                            <Setter TargetName="PART_SearchButton" Property="Background" Value="{DynamicResource WindowBackgroundBrush}" />
                                        </Trigger>-->
                        <Trigger SourceName="PART_SearchButton" Property="IsMouseOver" Value="True">
                            <Setter TargetName="PART_SearchButton" Property="Background" Value="{DynamicResource GrayBrush8}" />
                            <Setter TargetName="PART_SearchButton" Property="Foreground" Value="{DynamicResource AccentColorBrush}" />
                        </Trigger>
                        <Trigger SourceName="PART_SearchButton" Property="IsPressed" Value="True">
                            <Setter TargetName="PART_SearchButton" Property="Background" Value="{DynamicResource BlackBrush}" />
                            <Setter TargetName="PART_SearchButton" Property="Foreground" Value="{DynamicResource WhiteBrush}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>

        </Setter>

    </Style>
    <!--#endregion Metro-->

</ResourceDictionary>